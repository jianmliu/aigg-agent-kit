{
  "name": "@ai3-inference/core",
  "version": "0.0.1",
  "private": true,
  "description": "Shared types: InferenceProvider, Attestation, verifiability tiers, digest reference + cross-language test vectors (plan T3)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "vectors"
  ],
  "scripts": {
    "build": "tsc -p .",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "viem": "^2.54.6"
  },
  "devDependencies": {
    "@types/node": "^22.7.5"
  }
}
