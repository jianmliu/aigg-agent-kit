{
  "name": "@ai3-inference/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Auto EVM provider market — ServiceRegistry + InferenceLedger (moved from aigg-src per the 2026-07-08 extraction plan), deploy scripts, address book.",
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy:local": "hardhat run deploy/deploy.js --network localhost",
    "deploy:chronos": "hardhat run deploy/deploy.js --network chronos",
    "deploy:mainnet": "hardhat run deploy/deploy.js --network autoEvmMainnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.1.2",
    "@nomicfoundation/hardhat-ethers": "^3.1.3",
    "chai": "^4.5.0",
    "ethers": "^6.17.0",
    "hardhat": "^2.28.6"
  }
}
